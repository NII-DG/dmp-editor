# User stories

## Remarks

These user stories should be re-arranged because they are extracted from the implemented pages and dialogs.

## 1 Top page

```csv
ユーザーストーリー,ユースケース,ユーザーストーリー名,主語,動詞,対象,理由,結果,ユーザーストーリーの完了条件
[ユーザー]として、[GakuNin RDMのパーソナルアクセストークン]を[入力・送信]したい。なぜならば、[DMP EditorとGakuNin RDMの接続（疎通）を確立するため]。,外部システム連携認証,GRDMトークンによる認証,ユーザー,入力・送信,GakuNin RDMのパーソナルアクセストークン,DMP EditorとGakuNin RDMの接続（疎通）を確立するため,GRDMとの認証が成功し、アプリが利用可能になる,・トークン入力フォームが存在すること・「認証」ボタン押下により認証処理が走ること
[ユーザー]として、[トークン取得ページへのリンク]を[クリック]したい。なぜならば、[トークン発行画面（GRDM設定画面）へ迷わずにアクセスするため]。,ユーザー誘導・ヘルプ,トークン取得ページへの誘導,ユーザー,クリック,トークン取得ページへのリンク,トークン発行画面（GRDM設定画面）へ迷わずにアクセスするため,ブラウザでGakuNin RDMのトークン設定ページが開く,・画面上にhttps://rdm.nii.ac.jp/settings/tokens へのリンクが表示されていること
[ユーザー]として、[推奨される権限（スコープ）]を[確認]したい。なぜならば、[DMP Editorが正常に動作するために必要な権限（osf.full_write）を付与したトークンを作成するため]。,トークン作成支援,必須スコープの確認,ユーザー,確認,推奨される権限（スコープ）,DMP Editorが正常に動作するために必要な権限（osf.full_write）を付与したトークンを作成するため,適切な権限が付与されたトークンがGRDM側で作成される,・UI上に「osf.full_write の権限を付与してください」という明確な指示が表示されていること
[セキュリティを重視するユーザー]として、[入力したトークン]を[ブラウザのローカルストレージのみに保存]したい。なぜならば、[サーバー側へのトークン流出リスクを最小限に抑えるため]。,セキュリティ管理,トークンのローカル保存,セキュリティを重視するユーザー,ブラウザのローカルストレージのみに保存,入力したトークン,サーバー側へのトークン流出リスクを最小限に抑えるため,トークンがサーバーに送信・保存されず、ブラウザ内でのみ管理される,・システム仕様として、トークンがLocalStorageにのみ保存される実装となっていること
[ユーザー]として、[サポートポータルの情報]を[参照]したい。なぜならば、[パーソナルアクセストークンの仕様や作成方法について詳細を理解するため]。,ユーザー教育・サポート,サポートポータルへのアクセス,ユーザー,参照,サポートポータルの情報,パーソナルアクセストークンの仕様や作成方法について詳細を理解するため,GakuNin RDMのサポートページへアクセスできる,・「パーソナルアクセストークン
[ユーザー]として、[アプリのバージョン]を[確認]したい。なぜならば、[現在使用しているDMP Editorが最新版であるか、あるいは不具合報告時にバージョンを特定するため]。,システム情報確認,バージョン情報の視認,ユーザー,確認,アプリのバージョン,現在使用しているDMP Editorが最新版であるか、あるいは不具合報告時にバージョンを特定するため,バージョン番号を認識できる,・フッター等にバージョン情報（例：0.2.1）が表示されていること
```

## 2 Project list of the top page

```csv
ユーザーストーリー,ユースケース,ユーザーストーリー名,主語,動詞,対象,理由,結果,ユーザーストーリーの完了条件
[ユーザー]として、[自分のアカウントに紐づくDMPプロジェクトの一覧]を[閲覧]したい。なぜならば、[管理しているプロジェクトの状況を把握し、作業対象を選択するため]。,プロジェクト管理,プロジェクト一覧の閲覧,ユーザー,閲覧,自分のアカウントに紐づくDMPプロジェクトの一覧,管理しているプロジェクトの状況を把握し、作業対象を選択するため,GRDMアカウントに紐づくプロジェクト名、作成日、最終更新日が表形式で表示される ,・プロジェクト名、作成日、更新日が表示されていること・GRDM上のデータと同期が取れていること
[ユーザー]として、[新規DMP Projectを作成するボタン]を[クリック]したい。なぜならば、[新しい研究プロジェクトのためのデータマネジメントプランを作成し始めるため]。,プロジェクト作成,新規プロジェクト作成の開始,ユーザー,クリック,新規 DMP Project を作成するボタン,新しい研究プロジェクトのためのデータマネジメントプランを作成し始めるため,新規プロジェクト作成画面（またはダイアログ）へ遷移する,・画面上部に「新規 DMP Project を作成する」ボタンが存在し、押下可能であること 
[ユーザー]として、[特定のプロジェクトの「編集」ボタン]を[クリック]したい。なぜならば、[既存のDMPの内容を修正・更新するため]。,プロジェクト編集,既存プロジェクトの編集開始,ユーザー,クリック,特定のプロジェクトの「編集」ボタン,既存のDMPの内容を修正・更新するため,対象プロジェクトの編集画面（エディター）が開く,・各プロジェクト行の右側に「編集」ボタンが存在すること ・押下すると該当プロジェクトの編集画面へ遷移すること
[ユーザー]として、[プロジェクト名の横にある外部リンクアイコン]を[クリック]したい。なぜならば、[GakuNin RDM本体上の当該プロジェクトページにアクセスし、元データや詳細設定を確認するため]。,外部参照,GRDMプロジェクトページへのアクセス,ユーザー,クリック,プロジェクト名の横にある外部リンクアイコン,GakuNin RDM本体上の当該プロジェクトページにアクセスし、元データや詳細設定を確認するため,新しいタブ等でGakuNin RDMの該当プロジェクトページが開く,・プロジェクト名の横に外部リンク（別窓）を示すアイコンがあること ・リンク先が正しいGRDMのURLであること
[ユーザー]として、[最終更新日]を[確認]したい。なぜならば、[直近で作業していたプロジェクトを特定したり、プロジェクトの鮮度を判断したりするため]。,情報確認,更新日時の確認,ユーザー,確認,最終更新日,直近で作業していたプロジェクトを特定したり、プロジェクトの鮮度を判断したりするため,各プロジェクトの正確な最終更新日時（秒単位）が確認できる,・「2025/09/30 04:48:28」のように年月日時分秒が表示されていること 
[ユーザー]として、[ログイン中のユーザー名]を[確認]したい。なぜならば、[正しいアカウント（GRDMアカウント）で接続されているかを認識するため]。,状態確認,ログインユーザーの確認,ユーザー,確認,ログイン中のユーザー名,正しいアカウント（GRDMアカウント）で接続されているかを認識するため,ヘッダー付近に自身の氏名が表示されている,・画面右上にユーザー名（例：Toshiyuki Nishiyama Hiraki）が表示されていること 
```

## 3 Create a new DMP project

```csv
ユーザーストーリー,ユースケース,ユーザーストーリー名,主語,動詞,対象,理由,結果,ユーザーストーリーの完了条件
[ユーザー]として、[DMP用のGRDMプロジェクト名]を[入力]したい。なぜならば、[DMPデータを格納するための新しいプロジェクトをGRDM上に作成するため]。,新規作成,プロジェクト名の入力（自動プレフィックス）,ユーザー,入力,DMP用のGRDMプロジェクト名,DMPデータを格納するための新しいプロジェクトをGRDM上に作成するため,入力した名称に dmp-project- のプレフィックスが付与された状態で認識される,・入力欄にプロジェクト名が入力されていること・プレフィックスの仕様がUI上で明示されていること
[ユーザー]として、[必須項目（提出日・プロジェクト情報・資金配分機関など）]を[入力・保存]したい。なぜならば、[DMPとして求められる書式要件を満たすため]。,基本情報編集,メタデータの入力と保存,ユーザー,入力・保存,必須項目（提出日・プロジェクト情報・資金配分機関など）,DMPとして求められる書式要件を満たすため,DMPの基本情報がシステム（GRDM）に保存される,・「*」がついた必須項目が全て埋まっていること・「GRDMに保存する」ボタンで保存が完了すること
[ユーザー]として、[共同研究者などの担当者]を[追加・並べ替え]したい。なぜならば、[実際の研究体制に合わせて、DMP上の役割分担を正確に記載するため]。,担当者管理,担当者の追加と順序変更,ユーザー,追加・並べ替え,共同研究者などの担当者,実際の研究体制に合わせて、DMP上の役割分担を正確に記載するため,担当者リストに行が追加され、役割・名前・ID等が記録される,・「+ 担当者を追加する」ボタンで行が追加できること・Up/Down操作等で表示順を変更できること
[ユーザー]として、[管理対象となる研究データ]を[定義（リスト追加）]したい。なぜならば、[本プロジェクトでどのようなデータを扱うかを計画書に明記するため]。,データ定義,研究データ情報の登録,ユーザー,定義（リスト追加）,管理対象となる研究データ,本プロジェクトでどのようなデータを扱うかを計画書に明記するため,研究データ情報テーブルに新しいデータ項目（名称・分野・種別）が追加される,・「+ データを追加する」ボタンで行が追加できること
[ユーザー]として、[既存のGRDMプロジェクトの一覧]から[特定のプロジェクト]を[関連付け（選択）]したい。なぜならば、[DMPの計画対象となる実データが格納されている場所（GRDMプロジェクト）を特定するため]。,外部連携設定,GRDMプロジェクトとの関連付け,ユーザー,関連付け（選択）,既存のGRDMプロジェクトの一覧,DMPの計画対象となる実データが格納されている場所（GRDMプロジェクト）を特定するため,DMPプロジェクトと既存のGRDMプロジェクトがリンクされる,・一覧の「関連付ける」ボタンを押下できること・関連付け完了後、ステータスが変化するか、関連付け済み情報が表示されること
[ユーザー]として、[作成したDMP]を[ファイルとして出力]したい。なぜならば、[資金配分機関への提出や、オフラインでの確認に使用するため]。,帳票出力,DMPのエクスポート,ユーザー,ファイルとして出力,作成したDMP,資金配分機関への提出や、オフラインでの確認に使用するため,PDF等の形式でDMPファイルがダウンロードされる,・「DMPを出力する」ボタンを押下し、ファイル保存ダイアログが開くこと
[ユーザー]として、[GRDM上のファイル]と[DMP上のデータ項目]を[紐付け]たい。なぜならば、[計画（DMP）と実態（ファイル）のトレーサビリティを確保するため]。,外部連携設定,実ファイルとのマッピング,ユーザー,紐付け,GRDM上のファイルとDMP上のデータ項目,計画（DMP）と実態（ファイル）のトレーサビリティを確保するため,DMP上のデータ定義に対して、具体的なGRDM内のファイルパスが記録される,・GRDMプロジェクトを関連付けた後に、ファイル選択UIが有効化されること（現状は「関連付けられたProjectがありません」と表示）
```

### 3-1 Modal adding/editing a collaborator

```csv
ユーザーストーリー,ユースケース,ユーザーストーリー名,主語,動詞,対象,理由,結果,ユーザーストーリーの完了条件
[ユーザー]として、[担当者の基本情報（役割・氏名・所属）]を[入力・追加]したい。なぜならば、[DMP上で誰がどのような役割で参画するかを定義するため]。,担当者管理,担当者の新規追加,ユーザー,入力・追加,担当者の基本情報（役割・氏名・所属）,DMP上で誰がどのような役割で参画するかを定義するため,入力した担当者情報が親画面のリストに追加され、モーダルが閉じる,・必須項目（役割、性、名、所属機関）を入力し、「追加」ボタンを押下できること 
[ユーザー]として、[研究者識別ID（e-Rad番号、ORCID）]を[入力]したい。なぜならば、[同姓同名の研究者と区別し、外部システムとの連携や本人確認を確実にするため]。,詳細情報入力,研究者IDの登録,ユーザー,入力,研究者識別ID（e-Rad番号、ORCID）,同姓同名の研究者と区別し、外部システムとの連携や本人確認を確実にするため,担当者情報の一部として各IDが保存される,・e-Rad番号およびORCIDの入力フィールドが存在し、英数字が入力可能であること 
[ユーザー]として、[入力操作]を[キャンセル]したい。なぜならば、[誤ってモーダルを開いた場合や、情報の追加を取りやめるため]。,操作キャンセル,追加の中止,ユーザー,キャンセル,入力操作,誤ってモーダルを開いた場合や、情報の追加を取りやめるため,入力内容は保存されず、モーダルが閉じて元の画面に戻る,・「キャンセル」ボタンを押下すると、リストに変更を加えずモーダルが閉じること 
[ユーザー]として、[必須項目]を[視覚的に識別]したい。なぜならば、[入力漏れによる手戻りを防ぎ、最低限必要な情報を確実に登録するため]。,入力補助,必須項目の確認,ユーザー,視覚的に識別,必須項目,入力漏れによる手戻りを防ぎ、最低限必要な情報を確実に登録するため,どの項目が必須であるかが一目でわかる,・「役割」「性」「名」「所属機関」のラベルにアスタリスク（*）が付与されていること
```

### 3-2 Modal adding/editing a dataset

```csv
ユーザーストーリー,ユースケース,ユーザーストーリー名,主語,動詞,対象,理由,結果,ユーザーストーリーの完了条件
[ユーザー]として、[データの基本情報（名称・分野・種別など）]を[入力]したい。なぜならば、[プロジェクトで生成・収集される具体的なデータ資産を明確に定義するため]。,データセット管理,データセット基本情報の登録,ユーザー,入力,データの基本情報（名称・分野・種別など）,プロジェクトで生成・収集される具体的なデータ資産を明確に定義するため,データセットのアイデンティティ（何であるか）が確立される,・必須項目（名称、掲載日、分野、種別）が入力されていること・分野や種別が選択肢等から適切に選べること
[ユーザー]として、[機密情報・個人情報の有無と取扱方針]を[記述]したい。なぜならば、[法令や倫理指針（コンプライアンス）に準拠した安全な管理体制を示すため]。,セキュリティ・倫理設定,機微情報の管理方針定義,ユーザー,記述,機密情報・個人情報の有無と取扱方針,法令や倫理指針（コンプライアンス）に準拠した安全な管理体制を示すため,センシティブなデータの取り扱いルールが明文化される,・「機密情報の有無」のチェック/選択ができること・機微情報がある場合の具体的な対応策を入力できること
[ユーザー]として、[研究期間中および終了後のデータ保存場所（リポジトリ）]を[指定]したい。なぜならば、[データの消失を防ぎ、長期的な保存とアクセス性（FAIR原則）を担保するため]。,データ保存計画,保存場所（リポジトリ）の指定,ユーザー,指定,研究期間中および終了後のデータ保存場所（リポジトリ）,データの消失を防ぎ、長期的な保存とアクセス性（FAIR原則）を担保するため,データの所在（ストレージ、機関リポジトリ等）がライフサイクルごとに記録される,・研究活動時と活動終了後それぞれの保存先を入力できること・バックアップ方法についての記述欄があること
[ユーザー]として、[アクセス権と公開予定日]を[設定]したい。なぜならば、[資金配分機関のオープンサイエンス方針や、プロジェクトの知財戦略に沿った公開計画を立てるため]。,公開設定,アクセス権とスケジュールの設定,ユーザー,設定,アクセス権と公開予定日,資金配分機関のオープンサイエンス方針や、プロジェクトの知財戦略に沿った公開計画を立てるため,データがいつ、どのような条件で第三者に提供されるかが確定する,・アクセス権（公開/限定公開など）が選択できること・カレンダー等を用いて日付を正確に入力できること
[ユーザー]として、[各項目の入力例（プレースホルダー）]を[参照]したい。なぜならば、[「概略データ量」や「再利用情報」など、具体的にどのような記述が求められているかを理解しやすくするため]。,ユーザー入力支援,入力例によるガイダンス確認,ユーザー,参照,各項目の入力例（プレースホルダー）,「概略データ量」や「再利用情報」など、具体的にどのような記述が求められているかを理解しやすくするため,迷いなく適切なフォーマットや内容で入力を行える,"・各入力フィールド内に「e.g., 〇〇の実証における...」といった具体的な例文が表示されていること"
[ユーザー]として、[「?」アイコン（ヘルプ）]を[確認]したい。なぜならば、[専門用語や項目の詳細な定義について、その場で補足情報を得るため]。,ユーザー教育・ヘルプ,項目別ヘルプの参照,ユーザー,確認,「?」アイコン（ヘルプ）,専門用語や項目の詳細な定義について、その場で補足情報を得るため,ツールチップやポップアップで項目の説明が表示される,・ラベルの横にある「?」マークにカーソルを合わせるかクリックすると説明文が表示されること
```